# Local development — stress-test fixture (mocked registry, USA forum).
# Launched via: make local-stress-test
#
# Meilisearch runs in Docker (started by Makefile before overmind).
# Everything else runs natively.

hardhat:  cd blockchain && npx hardhat node
frontend: VITE_NETWORK=localhost VITE_REGISTRY_MODE=mocked VITE_SEARCH_RESULTS_LIMIT=100 DEPLOY_NETWORK=localhost_stress STRESS_TEST=1 STRESS_FORUM=USA ./scripts/local-deploy-and-serve.sh
backend:  NETWORK_FILE_NAME=localhost ./scripts/local-backend.sh