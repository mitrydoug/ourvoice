# Local development — mocked registry (no Sepolia fork).
# Launched via: make local-mocked
#
# Meilisearch runs in Docker under Overmind. Everything else runs natively.

meilisearch: bash scripts/dev/meilisearch.sh local-mocked
hardhat: bash scripts/dev/hardhat.sh local-mocked
contracts: bash scripts/dev/contracts.sh local-mocked
frontend: bash scripts/dev/frontend.sh local-mocked
backend: bash scripts/dev/backend.sh local-mocked
