# Vite production-build (`vite build`) overrides on top of env/.env.
# CI deploy profiles (env/profiles/{develop,release}.env) override these further.
VITE_REGISTRY_MODE=production
VITE_NETWORK=base
VITE_ALCHEMY_GAS_POLICY_ID=