# Docker Compose environment defaults (checked into git).
# For secrets, create a .env.local file (gitignored) with overrides.
#
# Example .env.local:
#   SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/your-key

COMPOSE_PROFILES=dev
COMPOSE_FILE=docker-compose.yml:docker-compose.mocked.yml